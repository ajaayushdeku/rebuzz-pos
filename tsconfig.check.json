{
  "//": "Typecheck config: `npx tsc -p tsconfig.check.json --noEmit`. Kept separate from tsconfig.json so a full check never writes tsbuildinfo or fights the dev server's incremental state.",
  "//next-env": "next-env.d.ts must NOT be excluded — it declares the image modules, and without it every `@/public/*.png` import reports TS2307.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "incremental": false
  },
  "exclude": ["node_modules", ".next"]
}
